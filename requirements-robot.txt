# Reachy Mini robot controller dependencies
# Shared runtime/audio deps live in requirements-common.txt so this file can
# stay focused on Reachy-specific additions.
# Option A (Pi + GCP): pip install -r requirements-robot.txt
# Option B (fully on Pi): pip install -r requirements.txt -r requirements-robot.txt

-r requirements-common.txt

# Robot SDK (Wireless version — Raspberry Pi 5)
reachy-mini>=1.2.0

# Synchronous HTTP client for robot scripts
requests>=2.31.0
